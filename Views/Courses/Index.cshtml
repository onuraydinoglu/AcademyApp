@model IEnumerable<Course>

<div class="flex justify-between">
    <div>
        <h2>Course List</h2>
    </div>

    <div>
        <a asp-controller="Courses" asp-action="Create" class="btn btn-outline-primary">Add Course</a>
    </div>
</div>

<div class="flex justify-center">
    <div class="w-[750px]">
        <table class="table text-center">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Category</th>
                    <th>Instructor</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var category in Model)
                {
                    <tr class="hover">
                        <th>@category.Id</th>
                        <td>@category.Category?.Name</td>
                        <td>@category.Instructor</td>
                        <td>@category.Title</td>
                        <td>@category.Description</td>
                        <td>
                            <a asp-controller="Categories" asp-action="Edit" asp-route-id="@category.Id"
                               class="btn btn-sm btn-warning">Edit</a>
                            <button class="btn btn-sm btn-error"
                                    onclick="my_modal_@(category.Id).showModal()">
                                Delete
                            </button>

                        </td>
                    </tr>
                    @await Html.PartialAsync("_Delete", category)
                }
            </tbody>
        </table>
    </div>
</div>